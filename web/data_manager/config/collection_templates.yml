templates:
- id: task_a_base
  task_id: Task_A
  subset: base
  prompt: Flatten and fold the cloth.
  enabled: true
  note: Task A base demo — prompt must match training config pi05_flatten_fold_normal
- id: task_a_dagger
  task_id: Task_A
  subset: dagger
  prompt: Flatten and fold the cloth.
  enabled: true
  note: Task A DAgger correction — same prompt as base
- id: task_e_base
  task_id: Task_E
  subset: base
  prompt: stand up the fallen box
  enabled: true
  note: TASK E BASE
- id: task_e_dagger
  task_id: Task_E
  subset: dagger
  prompt: stand up the fallen box
  enabled: true
  note: Task E DAgger correction — same prompt as base/training config pi05_stand_box_normal
